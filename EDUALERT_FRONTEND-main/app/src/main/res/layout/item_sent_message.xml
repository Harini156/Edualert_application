<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    card_view:cardCornerRadius="12dp"
    card_view:cardElevation="4dp"
    android:background="@drawable/input_bg">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Message Title -->
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Message Title"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="#000000"
            android:layout_alignParentStart="true"/>

        <!-- Delete Button -->
        <ImageView
            android:id="@+id/btnDelete"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentEnd="true"
            android:src="@drawable/ic_delete"
            android:contentDescription="Delete Message"
            android:tint="#922381"/>

        <!-- Description -->
        <TextView
            android:id="@+id/tvDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvTitle"
            android:layout_marginTop="6dp"
            android:text="Message Content"
            android:textSize="14sp"
            android:textColor="#444"/>

        <!-- Recipient Type -->
        <TextView
            android:id="@+id/tvRecipientType"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvDescription"
            android:layout_marginTop="4dp"
            android:text="Recipient: -"
            android:textSize="13sp"
            android:textColor="#666"/>

        <!-- Department -->
        <TextView
            android:id="@+id/tvDepartment"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvRecipientType"
            android:layout_marginTop="2dp"
            android:text="Department: -"
            android:textSize="13sp"
            android:textColor="#666"/>

        <!-- Attachment -->
        <TextView
            android:id="@+id/tvAttachment"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvDepartment"
            android:layout_marginTop="2dp"
            android:text="Attachment: -"
            android:textSize="13sp"
            android:textColor="#666"/>

        <!-- Staff Type -->
        <TextView
            android:id="@+id/tvStaffType"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvAttachment"
            android:layout_marginTop="2dp"
            android:text="Staff Type: -"
            android:textSize="13sp"
            android:textColor="#666"/>

        <!-- Designation -->
        <TextView
            android:id="@+id/tvDesignation"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvStaffType"
            android:layout_marginTop="2dp"
            android:text="Designation: -"
            android:textSize="13sp"
            android:textColor="#666"/>

        <!-- Year -->
        <TextView
            android:id="@+id/tvYear"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvDesignation"
            android:layout_marginTop="2dp"
            android:text="Year: -"
            android:textSize="13sp"
            android:textColor="#666"/>

        <!-- Stay Type -->
        <TextView
            android:id="@+id/tvStayType"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvYear"
            android:layout_marginTop="2dp"
            android:text="Stay Type: -"
            android:textSize="13sp"
            android:textColor="#666"/>

        <!-- Gender -->
        <TextView
            android:id="@+id/tvGender"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvStayType"
            android:layout_marginTop="2dp"
            android:text="Gender: -"
            android:textSize="13sp"
            android:textColor="#666"/>

        <!-- CGPA -->
        <TextView
            android:id="@+id/tvCgpa"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvGender"
            android:layout_marginTop="2dp"
            android:text="CGPA: -"
            android:textSize="13sp"
            android:textColor="#666"/>

        <!-- Backlogs -->
        <TextView
            android:id="@+id/tvBacklogs"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvCgpa"
            android:layout_marginTop="2dp"
            android:text="Backlogs: -"
            android:textSize="13sp"
            android:textColor="#666"/>

        <!-- Created At -->
        <TextView
            android:id="@+id/tvCreatedAt"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvBacklogs"
            android:layout_marginTop="4dp"
            android:text="Created At: -"
            android:textSize="12sp"
            android:textColor="#999"/>

    </RelativeLayout>
</androidx.cardview.widget.CardView>
