<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="30dp"
    android:background="@drawable/subscription_gradient_bg">

    <!-- App Name -->
    <TextView
        android:id="@+id/appNameText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="EduAlert"
        android:textSize="36sp"
        android:textStyle="bold"
        android:textColor="#922381"
        android:layout_marginBottom="20dp"
        android:fontFamily="sans-serif-medium" />

    <!-- Subtitle -->
    <TextView
        android:id="@+id/subtitleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Unlock Premium Features"
        android:textSize="18sp"
        android:textColor="#666666"
        android:layout_marginBottom="60dp"
        android:gravity="center" />

    <!-- Subscribe to Premium Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSubscribe"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:text="Subscribe to Premium"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:textStyle="bold"
        app:cornerRadius="14dp"
        app:backgroundTint="#922381"
        android:layout_marginBottom="20dp"
        android:elevation="4dp" />

    <!-- Maybe Later Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnMaybeLater"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:text="Maybe Later"
        android:textColor="#922381"
        android:textSize="16sp"
        app:cornerRadius="14dp"
        app:backgroundTint="@android:color/transparent"
        app:strokeColor="#922381"
        app:strokeWidth="2dp"
        android:elevation="0dp" />

</LinearLayout>
